이 패치 파일을 로컬에서 적용하세요:
git apply patches/patch-1-small.patch
git apply patches/patch-2-med.patch  
git apply patches/patch-3-big.patch

적용 후 cost.js, ai-api.js는 이미 브랜치에 반영되어 있으므로
나머지 8개 파일만 패치로 적용하면 됩니다.
